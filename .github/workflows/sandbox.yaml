name: Semantic Release

on:
  workflow_dispatch:
jobs:
  semantic-release:
    name: Semantic Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Install dependencies
        run: npm install
      - name: Install dependencies
        run: npm install
      - name: Semantic Release
        uses: OmarJaroudi/semantic-release-action@2cb02dd97cdb2ee5a0f3ef017a57a8d164b1d67f
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
